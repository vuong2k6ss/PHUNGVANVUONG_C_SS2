#include <stdio.h>

int main() 
{
    int length = 5; 
    int width = 4; 
    int P = (length + width) * 2; //chu vi hcn
    int S = length * width;  // dien tich hcn    
    printf("Chu vi hcn la: %d\n",P);
    printf("Dien tich hcn la: %d\n",S);
    return 0;
}

